{
  "name": "bird-sdk-node",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root for the Bird JS/TS packages mirror — not published; each package under packages/* publishes independently.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "build": "pnpm --filter \"./packages/*\" run build",
    "lint": "pnpm --filter \"./packages/*\" run lint",
    "test": "pnpm --filter \"./packages/*\" run test",
    "publint": "pnpm --filter \"./packages/*\" --no-bail exec publint --strict",
    "check:readme": "cmp -s README.md packages/sdk/README.md || { echo 'root README.md must be byte-identical to packages/sdk/README.md (single-sourced from the SDK)'; exit 1; }"
  },
  "devDependencies": {
    "publint": "^0.3.13"
  }
}
